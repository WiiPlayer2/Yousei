subscription OnTrigger_Subscribe($config: BlockConfigInput!) {
  onTrigger(config: $config) {
    string
  }
}

mutation Act($configs: [BlockConfigInput!]!) {
  act(config: $configs) {
    string
  }
}
